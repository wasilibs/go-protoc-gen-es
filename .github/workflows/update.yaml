name: "Update Upstream"

on:
  schedule:
    - cron: "5 4 * * *"
  workflow_dispatch:

permissions:
  id-token: write
  attestations: write

jobs:
  build:
    uses: wasilibs/actions/.github/workflows/update-node.yaml@09063a6e2f4da10fd51a2af75c6469c17c35a160
    with:
      packages: "@bufbuild/protoc-gen-es"
    secrets: inherit
