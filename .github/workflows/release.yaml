name: "Release"

on:
  push:
    tags:
      - v*

permissions:
  id-token: write
  attestations: write
  contents: write

jobs:
  release:
    uses: wasilibs/actions/.github/workflows/release.yaml@e3a87366ddfbb45ab5c1f8f90b7ee3a2fd652715
    secrets: inherit
