name: CI
on:
  push:
    branches:
      - main
    tags:
      - "*"
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: wasilibs/actions/.github/workflows/ci.yaml@e3a87366ddfbb45ab5c1f8f90b7ee3a2fd652715
    secrets: inherit
